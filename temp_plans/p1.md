KẾ HOẠCH XÂY DỰNG WEB SERVICE PHP - HỆ THỐNG TUYỂN SINH
📋 TỔNG QUAN DỰ ÁN

text

┌─────────────────────────────────────────────────────────────────────────────┐
│ HỆ THỐNG TUYỂN SINH SINH VIÊN │
├─────────────────────────────────────────────────────────────────────────────┤
│ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ │
│ │ GIAI ĐOẠN 1│ ───▶ │ GIAI ĐOẠN 2│ ───▶ │ GIAI ĐOẠN 3│ │
│ │ Đăng ký │ │ Xét duyệt │ │ Sau nhập học│ │
│ │ Nộp hồ sơ │ │ Đồng bộ │ │ Student │ │
│ │ PHP Portal │ │ PHP ↔ ASP │ │ Portal │ │
│ └─────────────┘ └─────────────┘ └─────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘

🏗️ PHẦN 1: KIẾN TRÚC HỆ THỐNG
1.1 Sơ đồ Kiến trúc Tổng thể

text

                                    ┌─────────────────┐
                                    │   INTERNET      │
                                    └────────┬────────┘
                                             │
                                    ┌────────▼────────┐
                                    │   CLOUDFLARE    │
                                    │   (CDN + WAF)   │
                                    └────────┬────────┘
                                             │
                              ┌──────────────┼──────────────┐
                              │              │              │
                    ┌─────────▼─────┐ ┌──────▼─────┐ ┌──────▼─────┐
                    │  WEB SERVER   │ │ API SERVER │ │ FILE SERVER│
                    │  (Frontend)   │ │ (Backend)  │ │ (Storage)  │
                    │  PHP + Blade  │ │ Laravel    │ │ S3/MinIO   │
                    └───────┬───────┘ └──────┬─────┘ └──────┬─────┘
                            │                │              │
                            └────────────────┼──────────────┘
                                             │
                    ┌────────────────────────┼────────────────────────┐
                    │                        │                        │
           ┌────────▼────────┐    ┌──────────▼──────────┐    ┌───────▼───────┐
           │   MySQL/MariaDB │    │   REDIS CACHE       │    │  QUEUE WORKER │
           │   (Primary DB)  │    │   (Session+Cache)   │    │  (Jobs)       │
           └─────────────────┘    └─────────────────────┘    └───────────────┘
                                             │
                                             │ SECURE VPN / API
                                             │
                              ┌──────────────▼──────────────┐
                              │      ASP.NET SYSTEM         │
                              │      (LAN Internal)         │
                              │  ┌─────────────────────┐    │
                              │  │  SQL Server DB      │    │
                              │  └─────────────────────┘    │
                              └─────────────────────────────┘

1.2 Tech Stack

YAML

Backend Framework:

- PHP 8.2+
- Laravel 11.x

Frontend:

- Blade Template Engine
- Tailwind CSS 3.x
- Alpine.js 3.x
- Livewire 3.x (cho interactive forms)

Database:

- MySQL 8.0 / MariaDB 10.11
- Redis 7.x (Cache & Session)

Queue & Jobs:

- Laravel Queue với Redis Driver
- Laravel Horizon (monitoring)

File Storage:

- Local Storage (development)
- AWS S3 / MinIO (production)

Authentication:

- Laravel Sanctum (API tokens)
- Custom OTP System

APIs & Integration:

- RESTful API (JSON)
- M-Pesa Payment Gateway
- SMS Gateway (Africa's Talking / Twilio)
- Email (SMTP / Mailgun)

DevOps:

- Docker + Docker Compose
- GitHub Actions (CI/CD)
- Nginx + PHP-FPM

📁 PHẦN 2: CẤU TRÚC THƯ MỤC DỰ ÁN

text

student-admission-portal/
│
├── 📁 app/
│ ├── 📁 Console/
│ │ └── Commands/
│ │ ├── SyncPendingApplications.php # Lệnh sync với ASP
│ │ ├── SendOtpReminder.php
│ │ └── CleanupExpiredOtp.php
│ │
│ ├── 📁 Enums/
│ │ ├── ApplicationStatus.php # Enum trạng thái hồ sơ
│ │ ├── PaymentStatus.php
│ │ └── UserRole.php
│ │
│ ├── 📁 Events/
│ │ ├── ApplicationSubmitted.php
│ │ ├── ApplicationApproved.php
│ │ ├── ApplicationRejected.php
│ │ └── PaymentReceived.php
│ │
│ ├── 📁 Exceptions/
│ │ ├── OtpExpiredException.php
│ │ ├── PaymentVerificationException.php
│ │ └── ApiAuthenticationException.php
│ │
│ ├── 📁 Http/
│ │ ├── 📁 Controllers/
│ │ │ ├── 📁 Api/
│ │ │ │ └── V1/
│ │ │ │ ├── StudentController.php # API cho ASP
│ │ │ │ ├── StatusController.php
│ │ │ │ ├── DocumentController.php
│ │ │ │ └── WebhookController.php
│ │ │ │
│ │ │ ├── 📁 Auth/
│ │ │ │ ├── RegisterController.php
│ │ │ │ ├── LoginController.php
│ │ │ │ ├── OtpController.php
│ │ │ │ └── PasswordController.php
│ │ │ │
│ │ │ ├── 📁 Student/
│ │ │ │ ├── DashboardController.php
│ │ │ │ ├── ApplicationController.php
│ │ │ │ ├── PaymentController.php
│ │ │ │ ├── DocumentController.php
│ │ │ │ ├── TimetableController.php
│ │ │ │ ├── GradeController.php
│ │ │ │ └── FeeController.php
│ │ │ │
│ │ │ └── 📁 Admin/
│ │ │ ├── DashboardController.php
│ │ │ └── ReportController.php
│ │ │
│ │ ├── 📁 Middleware/
│ │ │ ├── ApiAuthentication.php # API key validation
│ │ │ ├── CheckApplicationStatus.php
│ │ │ ├── RateLimitOtp.php
│ │ │ └── EnsureApprovedStudent.php
│ │ │
│ │ ├── 📁 Requests/
│ │ │ ├── 📁 Auth/
│ │ │ │ ├── RegisterRequest.php
│ │ │ │ ├── VerifyOtpRequest.php
│ │ │ │ └── LoginRequest.php
│ │ │ │
│ │ │ ├── 📁 Application/
│ │ │ │ ├── PersonalInfoRequest.php
│ │ │ │ ├── ParentInfoRequest.php
│ │ │ │ ├── ProgramSelectionRequest.php
│ │ │ │ ├── DocumentUploadRequest.php
│ │ │ │ └── PaymentSubmissionRequest.php
│ │ │ │
│ │ │ └── 📁 Api/
│ │ │ └── UpdateStatusRequest.php
│ │ │
│ │ └── 📁 Resources/
│ │ ├── StudentResource.php
│ │ ├── ApplicationResource.php
│ │ ├── StudentCollection.php
│ │ └── GradeResource.php
│ │
│ ├── 📁 Jobs/
│ │ ├── SendOtpEmail.php
│ │ ├── SendOtpSms.php
│ │ ├── SendApplicationConfirmation.php
│ │ ├── SendApprovalNotification.php
│ │ ├── SyncApplicationToAsp.php
│ │ └── ProcessPaymentVerification.php
│ │
│ ├── 📁 Listeners/
│ │ ├── SendSubmissionEmail.php
│ │ ├── NotifyAdmissionTeam.php
│ │ └── UpdateStudentStatus.php
│ │
│ ├── 📁 Mail/
│ │ ├── OtpVerification.php
│ │ ├── ApplicationReceived.php
│ │ ├── ApplicationApproved.php
│ │ ├── ApplicationRejected.php
│ │ └── RequestMoreInfo.php
│ │
│ ├── 📁 Models/
│ │ ├── User.php
│ │ ├── Student.php
│ │ ├── Application.php
│ │ ├── ApplicationStep.php
│ │ ├── Document.php
│ │ ├── Payment.php
│ │ ├── Otp.php
│ │ ├── Program.php
│ │ ├── AcademicBlock.php
│ │ ├── StatusHistory.php
│ │ └── ApiLog.php
│ │
│ ├── 📁 Notifications/
│ │ ├── OtpNotification.php
│ │ └── StatusChangeNotification.php
│ │
│ ├── 📁 Policies/
│ │ ├── ApplicationPolicy.php
│ │ └── DocumentPolicy.php
│ │
│ ├── 📁 Providers/
│ │ ├── AppServiceProvider.php
│ │ ├── EventServiceProvider.php
│ │ └── AuthServiceProvider.php
│ │
│ ├── 📁 Services/
│ │ ├── 📁 Auth/
│ │ │ ├── OtpService.php
│ │ │ └── AuthenticationService.php
│ │ │
│ │ ├── 📁 Application/
│ │ │ ├── ApplicationService.php
│ │ │ ├── StepValidationService.php
│ │ │ └── DocumentService.php
│ │ │
│ │ ├── 📁 Payment/
│ │ │ ├── MpesaService.php
│ │ │ └── PaymentVerificationService.php
│ │ │
│ │ ├── 📁 Integration/
│ │ │ ├── AspApiService.php # Giao tiếp với ASP
│ │ │ ├── SmsService.php
│ │ │ └── EmailService.php
│ │ │
│ │ └── 📁 Student/
│ │ ├── TimetableService.php
│ │ ├── GradeService.php
│ │ └── FeeService.php
│ │
│ └── 📁 Traits/
│ ├── HasApplicationStatus.php
│ ├── Auditable.php
│ └── ApiResponseTrait.php
│
├── 📁 bootstrap/
├── 📁 config/
│ ├── admission.php # Config tuyển sinh
│ ├── mpesa.php # Config M-Pesa
│ ├── asp_integration.php # Config kết nối ASP
│ └── otp.php # Config OTP
│
├── 📁 database/
│ ├── 📁 factories/
│ ├── 📁 migrations/
│ │ ├── 2024_01_01_000001_create_users_table.php
│ │ ├── 2024_01_01_000002_create_otps_table.php
│ │ ├── 2024_01_01_000003_create_students_table.php
│ │ ├── 2024_01_01_000004_create_programs_table.php
│ │ ├── 2024_01_01_000005_create_academic_blocks_table.php
│ │ ├── 2024_01_01_000006_create_applications_table.php
│ │ ├── 2024_01_01_000007_create_application_steps_table.php
│ │ ├── 2024_01_01_000008_create_documents_table.php
│ │ ├── 2024_01_01_000009_create_payments_table.php
│ │ ├── 2024_01_01_000010_create_status_histories_table.php
│ │ └── 2024_01_01_000011_create_api_logs_table.php
│ │
│ └── 📁 seeders/
│ ├── DatabaseSeeder.php
│ ├── ProgramSeeder.php
│ └── AcademicBlockSeeder.php
│
├── 📁 public/
│ ├── index.php
│ ├── 📁 css/
│ ├── 📁 js/
│ └── 📁 images/
│
├── 📁 resources/
│ ├── 📁 css/
│ │ └── app.css
│ │
│ ├── 📁 js/
│ │ ├── app.js
│ │ └── 📁 components/
│ │ ├── otp-input.js
│ │ ├── file-upload.js
│ │ └── step-form.js
│ │
│ └── 📁 views/
│ ├── 📁 layouts/
│ │ ├── app.blade.php
│ │ ├── auth.blade.php
│ │ └── student.blade.php
│ │
│ ├── 📁 auth/
│ │ ├── register.blade.php
│ │ ├── verify-otp.blade.php
│ │ ├── login.blade.php
│ │ └── forgot-password.blade.php
│ │
│ ├── 📁 application/
│ │ ├── index.blade.php
│ │ ├── 📁 steps/
│ │ │ ├── personal-info.blade.php
│ │ │ ├── parent-info.blade.php
│ │ │ ├── program-selection.blade.php
│ │ │ └── document-upload.blade.php
│ │ ├── payment.blade.php
│ │ ├── review.blade.php
│ │ └── success.blade.php
│ │
│ ├── 📁 student/
│ │ ├── dashboard.blade.php
│ │ ├── timetable.blade.php
│ │ ├── grades.blade.php
│ │ ├── fees.blade.php
│ │ └── profile.blade.php
│ │
│ ├── 📁 components/
│ │ ├── alert.blade.php
│ │ ├── button.blade.php
│ │ ├── input.blade.php
│ │ ├── file-upload.blade.php
│ │ ├── step-indicator.blade.php
│ │ └── status-badge.blade.php
│ │
│ ├── 📁 emails/
│ │ ├── otp.blade.php
│ │ ├── application-received.blade.php
│ │ ├── application-approved.blade.php
│ │ └── request-info.blade.php
│ │
│ └── 📁 livewire/
│ ├── otp-form.blade.php
│ ├── application-wizard.blade.php
│ └── document-uploader.blade.php
│
├── 📁 routes/
│ ├── web.php # Routes web
│ ├── api.php # API routes cho ASP
│ ├── auth.php # Auth routes
│ └── student.php # Student portal routes
│
├── 📁 storage/
│ └── 📁 app/
│ ├── 📁 documents/ # Lưu trữ tài liệu
│ └── 📁 payments/ # Lưu trữ chứng từ
│
├── 📁 tests/
│ ├── 📁 Feature/
│ │ ├── 📁 Auth/
│ │ │ ├── RegistrationTest.php
│ │ │ ├── OtpVerificationTest.php
│ │ │ └── LoginTest.php
│ │ │
│ │ ├── 📁 Application/
│ │ │ ├── ApplicationSubmissionTest.php
│ │ │ └── PaymentSubmissionTest.php
│ │ │
│ │ └── 📁 Api/
│ │ ├── StudentApiTest.php
│ │ └── StatusUpdateApiTest.php
│ │
│ └── 📁 Unit/
│ ├── OtpServiceTest.php
│ ├── MpesaServiceTest.php
│ └── ApplicationServiceTest.php
│
├── 📁 docker/
│ ├── Dockerfile
│ ├── nginx/
│ │ └── default.conf
│ └── php/
│ └── php.ini
│
├── .env.example
├── .gitignore
├── composer.json
├── docker-compose.yml
├── package.json
├── phpunit.xml
├── tailwind.config.js
├── vite.config.js
└── README.md

🗄️ PHẦN 3: THIẾT KẾ DATABASE
3.1 Entity Relationship Diagram

text

┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│ users │ │ students │ │ applications │
├─────────────────┤ ├─────────────────┤ ├─────────────────┤
│ id │──┐ │ id │──┐ │ id │
│ email │ │ │ user_id (FK) │──┘ │ student_id (FK) │──┐
│ phone │ └───▶│ student_code │◀──────│ program_id (FK) │ │
│ password │ │ first_name │ │ block_id (FK) │ │
│ email_verified │ │ last_name │ │ status │ │
│ phone_verified │ │ date_of_birth │ │ current_step │ │
│ status │ │ gender │ │ submitted_at │ │
│ created_at │ │ nationality │ │ created_at │ │
│ updated_at │ │ address │ │ updated_at │ │
└─────────────────┘ │ created_at │ └─────────────────┘
│ updated_at │ │
└─────────────────┘ │
│ │
▼ ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│ parent_info │ │ documents │ │application_steps│
├─────────────────┤ ├─────────────────┤ ├─────────────────┤
│ id │ │ id │ │ id │
│ student_id (FK) │──┐ │ application_id │───────│ application_id │
│ relation_type │ │ │ type │ │ step_number │
│ full_name │ │ │ file_path │ │ step_name │
│ phone │ │ │ original_name │ │ data (JSON) │
│ email │ │ │ status │ │ is_completed │
│ occupation │ │ │ created_at │ │ completed_at │
│ address │ │ │ updated_at │ │ created_at │
│ created_at │ │ └─────────────────┘ │ updated_at │
│ updated_at │ │ └─────────────────┘
└─────────────────┘ │
│
┌─────────────────┐ │ ┌─────────────────┐ ┌─────────────────┐
│ payments │ │ │ programs │ │ academic_blocks │
├─────────────────┤ │ ├─────────────────┤ ├─────────────────┤
│ id │ │ │ id │ │ id │
│ application_id │──┘ │ code │ │ name │
│ amount │ │ name │ │ year │
│ mpesa_code │ │ description │ │ intake │
│ proof_file │ │ duration │ │ start_date │
│ status │ │ fee │ │ end_date │
│ verified_at │ │ is_active │ │ is_active │
│ verified_by │ │ created_at │ │ created_at │
│ created_at │ │ updated_at │ │ updated_at │
│ updated_at │ └─────────────────┘ └─────────────────┘
└─────────────────┘

┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│ otps │ │ status_histories│ │ api_logs │
├─────────────────┤ ├─────────────────┤ ├─────────────────┤
│ id │ │ id │ │ id │
│ user_id (FK) │ │ application_id │ │ endpoint │
│ otp_code │ │ from_status │ │ method │
│ type (email/sms)│ │ to_status │ │ request_body │
│ purpose │ │ changed_by │ │ response_body │
│ expires_at │ │ notes │ │ status_code │
│ verified_at │ │ source (PHP/ASP)│ │ ip_address │
│ attempts │ │ created_at │ │ api_key │
│ created_at │ └─────────────────┘ │ created_at │
└─────────────────┘ └─────────────────┘

3.2 Migration Files

PHP

<?php
// database/migrations/2024_01_01_000001_create_users_table.php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::create('users', function (Blueprint $table) {
            $table->id();
            $table->string('email')->unique()->nullable();
            $table->string('phone', 20)->unique()->nullable();
            $table->timestamp('email_verified_at')->nullable();
            $table->timestamp('phone_verified_at')->nullable();
            $table->string('password');
            $table->enum('status', ['new', 'active', 'suspended'])->default('new');
            $table->rememberToken();
            $table->timestamps();
            $table->softDeletes();
            
            $table->index(['email', 'phone']);
            $table->index('status');
        });
    }
};

PHP

<?php
// database/migrations/2024_01_01_000002_create_otps_table.php

return new class extends Migration
{
    public function up(): void
    {
        Schema::create('otps', function (Blueprint $table) {
            $table->id();
            $table->foreignId('user_id')->nullable()->constrained()->cascadeOnDelete();
            $table->string('identifier'); // email hoặc phone
            $table->string('otp_code', 6);
            $table->enum('type', ['email', 'sms']);
            $table->enum('purpose', ['registration', 'login', 'password_reset']);
            $table->timestamp('expires_at');
            $table->timestamp('verified_at')->nullable();
            $table->tinyInteger('attempts')->default(0);
            $table->timestamps();
            
            $table->index(['identifier', 'otp_code', 'purpose']);
            $table->index('expires_at');
        });
    }
};

PHP

<?php
// database/migrations/2024_01_01_000003_create_students_table.php

return new class extends Migration
{
    public function up(): void
    {
        Schema::create('students', function (Blueprint $table) {
            $table->id();
            $table->foreignId('user_id')->constrained()->cascadeOnDelete();
            $table->string('student_code', 20)->unique()->nullable(); // Cấp sau khi approved
            $table->string('first_name', 100);
            $table->string('middle_name', 100)->nullable();
            $table->string('last_name', 100);
            $table->date('date_of_birth');
            $table->enum('gender', ['male', 'female', 'other']);
            $table->string('nationality', 100)->default('Kenya');
            $table->string('national_id', 50)->nullable();
            $table->string('passport_number', 50)->nullable();
            $table->text('address');
            $table->string('city', 100);
            $table->string('county', 100)->nullable();
            $table->string('postal_code', 20)->nullable();
            $table->string('profile_photo')->nullable();
            $table->timestamps();
            $table->softDeletes();
            
            $table->index('student_code');
            $table->index(['first_name', 'last_name']);
        });
    }
};

PHP

<?php
// database/migrations/2024_01_01_000006_create_applications_table.php

return new class extends Migration
{
    public function up(): void
    {
        Schema::create('applications', function (Blueprint $table) {
            $table->id();
            $table->foreignId('student_id')->constrained()->cascadeOnDelete();
            $table->foreignId('program_id')->constrained();
            $table->foreignId('block_id')->constrained('academic_blocks');
            $table->string('application_number', 30)->unique();
            $table->enum('status', [
                'draft',           // Đang điền
                'pending_payment', // Chờ thanh toán
                'pending_approval',// Chờ xét duyệt
                'request_info',    // Yêu cầu bổ sung
                'approved',        // Đã duyệt
                'rejected'         // Từ chối
            ])->default('draft');
            $table->tinyInteger('current_step')->default(1);
            $table->tinyInteger('total_steps')->default(4);
            $table->text('admin_notes')->nullable();
            $table->text('rejection_reason')->nullable();
            $table->timestamp('submitted_at')->nullable();
            $table->timestamp('approved_at')->nullable();
            $table->string('approved_by')->nullable();
            $table->timestamps();
            $table->softDeletes();
            
            $table->index('application_number');
            $table->index('status');
            $table->index('submitted_at');
        });
    }
};

PHP

<?php
// database/migrations/2024_01_01_000009_create_payments_table.php

return new class extends Migration
{
    public function up(): void
    {
        Schema::create('payments', function (Blueprint $table) {
            $table->id();
            $table->foreignId('application_id')->constrained()->cascadeOnDelete();
            $table->decimal('amount', 10, 2);
            $table->string('currency', 3)->default('KES');
            $table->string('mpesa_code', 20)->nullable();
            $table->string('mpesa_phone', 20)->nullable();
            $table->string('proof_file')->nullable();
            $table->enum('status', [
                'pending',
                'submitted',
                'verified',
                'rejected'
            ])->default('pending');
            $table->text('verification_notes')->nullable();
            $table->timestamp('verified_at')->nullable();
            $table->string('verified_by')->nullable();
            $table->timestamps();
            
            $table->index('mpesa_code');
            $table->index('status');
        });
    }
};

🔌 PHẦN 4: API ENDPOINTS
4.1 API Routes

PHP

<?php
// routes/api.php

use Illuminate\Support\Facades\Route;
use App\Http\Controllers\Api\V1\StudentController;
use App\Http\Controllers\Api\V1\StatusController;
use App\Http\Controllers\Api\V1\DocumentController;
use App\Http\Controllers\Api\V1\WebhookController;

/*
|--------------------------------------------------------------------------
| API Routes cho ASP.NET System
|--------------------------------------------------------------------------
*/

Route::prefix('v1')->middleware(['api.auth', 'throttle:api'])->group(function () {
    
    // ═══════════════════════════════════════════════════════════════
    // STUDENTS - Lấy danh sách sinh viên/hồ sơ
    // ═══════════════════════════════════════════════════════════════
    
    // GET /api/v1/students?status=pending_approval&page=1&per_page=50
    Route::get('/students', [StudentController::class, 'index'])
        ->name('api.students.index');
    
    // GET /api/v1/students/{id}
    Route::get('/students/{id}', [StudentController::class, 'show'])
        ->name('api.students.show');
    
    // GET /api/v1/students/{id}/documents
    Route::get('/students/{id}/documents', [DocumentController::class, 'index'])
        ->name('api.students.documents');
    
    // GET /api/v1/documents/{id}/download
    Route::get('/documents/{id}/download', [DocumentController::class, 'download'])
        ->name('api.documents.download');
    
    // ═══════════════════════════════════════════════════════════════
    // STATUS UPDATE - Cập nhật trạng thái từ ASP
    // ═══════════════════════════════════════════════════════════════
    
    // POST /api/v1/update-status
    Route::post('/update-status', [StatusController::class, 'update'])
        ->name('api.status.update');
    
    // POST /api/v1/bulk-update-status
    Route::post('/bulk-update-status', [StatusController::class, 'bulkUpdate'])
        ->name('api.status.bulk-update');
    
    // ═══════════════════════════════════════════════════════════════
    // STUDENT DATA - Dữ liệu cho Student Portal
    // ═══════════════════════════════════════════════════════════════
    
    // GET /api/v1/students/{student_code}/grades
    Route::get('/students/{student_code}/grades', [StudentController::class, 'grades'])
        ->name('api.students.grades');
    
    // GET /api/v1/students/{student_code}/timetable
    Route::get('/students/{student_code}/timetable', [StudentController::class, 'timetable'])
        ->name('api.students.timetable');
    
    // GET /api/v1/students/{student_code}/fees
    Route::get('/students/{student_code}/fees', [StudentController::class, 'fees'])
        ->name('api.students.fees');
    
    // ═══════════════════════════════════════════════════════════════
    // WEBHOOKS - Nhận thông báo từ ASP
    // ═══════════════════════════════════════════════════════════════
    
    Route::post('/webhooks/status-changed', [WebhookController::class, 'statusChanged'])
        ->name('api.webhooks.status');
    
    Route::post('/webhooks/grade-updated', [WebhookController::class, 'gradeUpdated'])
        ->name('api.webhooks.grade');
});

// Health check endpoint (không cần auth)
Route::get('/health', function () {
    return response()->json([
        'status' => 'ok',
        'timestamp' => now()->toIso8601String(),
        'version' => config('app.version', '1.0.0')
    ]);
});

4.2 API Controllers

PHP

<?php
// app/Http/Controllers/Api/V1/StudentController.php

namespace App\Http\Controllers\Api\V1;

use App\Http\Controllers\Controller;
use App\Http\Resources\StudentResource;
use App\Http\Resources\StudentCollection;
use App\Models\Application;
use App\Models\Student;
use App\Services\Integration\AspApiService;
use Illuminate\Http\Request;
use Illuminate\Http\JsonResponse;

class StudentController extends Controller
{
    public function __construct(
        private AspApiService $aspService
    ) {}

    /**
     * Lấy danh sách sinh viên theo trạng thái
     * 
     * GET /api/v1/students?status=pending_approval&page=1&per_page=50
     */
    public function index(Request $request): JsonResponse
    {
        $validated = $request->validate([
            'status' => 'nullable|in:draft,pending_payment,pending_approval,request_info,approved,rejected',
            'from_date' => 'nullable|date',
            'to_date' => 'nullable|date|after_or_equal:from_date',
            'page' => 'nullable|integer|min:1',
            'per_page' => 'nullable|integer|min:1|max:100',
        ]);

        $query = Application::with([
            'student',
            'student.parentInfo',
            'program',
            'block',
            'documents',
            'payment'
        ]);

        // Filter by status
        if (isset($validated['status'])) {
            $query->where('status', $validated['status']);
        }

        // Filter by date range
        if (isset($validated['from_date'])) {
            $query->where('submitted_at', '>=', $validated['from_date']);
        }
        if (isset($validated['to_date'])) {
            $query->where('submitted_at', '<=', $validated['to_date']);
        }

        $perPage = $validated['per_page'] ?? 50;
        $applications = $query->orderBy('submitted_at', 'desc')
                             ->paginate($perPage);

        return response()->json([
            'success' => true,
            'data' => StudentResource::collection($applications),
            'meta' => [
                'current_page' => $applications->currentPage(),
                'last_page' => $applications->lastPage(),
                'per_page' => $applications->perPage(),
                'total' => $applications->total(),
            ]
        ]);
    }

    /**
     * Lấy chi tiết một hồ sơ
     * 
     * GET /api/v1/students/{id}
     */
    public function show(int $id): JsonResponse
    {
        $application = Application::with([
            'student',
            'student.parentInfo',
            'program',
            'block',
            'documents',
            'payment',
            'steps',
            'statusHistories'
        ])->findOrFail($id);

        return response()->json([
            'success' => true,
            'data' => new StudentResource($application)
        ]);
    }

    /**
     * Lấy điểm sinh viên từ ASP
     * 
     * GET /api/v1/students/{student_code}/grades
     */
    public function grades(string $studentCode): JsonResponse
    {
        $student = Student::where('student_code', $studentCode)
                         ->whereHas('application', fn($q) => $q->where('status', 'approved'))
                         ->firstOrFail();

        // Gọi API ASP để lấy điểm
        $grades = $this->aspService->getStudentGrades($studentCode);

        return response()->json([
            'success' => true,
            'data' => $grades
        ]);
    }

    /**
     * Lấy thời khóa biểu từ ASP
     * 
     * GET /api/v1/students/{student_code}/timetable
     */
    public function timetable(string $studentCode): JsonResponse
    {
        $student = Student::where('student_code', $studentCode)
                         ->whereHas('application', fn($q) => $q->where('status', 'approved'))
                         ->firstOrFail();

        $timetable = $this->aspService->getStudentTimetable($studentCode);

        return response()->json([
            'success' => true,
            'data' => $timetable
        ]);
    }

    /**
     * Lấy thông tin học phí từ ASP
     * 
     * GET /api/v1/students/{student_code}/fees
     */
    public function fees(string $studentCode): JsonResponse
    {
        $student = Student::where('student_code', $studentCode)
                         ->whereHas('application', fn($q) => $q->where('status', 'approved'))
                         ->firstOrFail();

        $fees = $this->aspService->getStudentFees($studentCode);

        return response()->json([
            'success' => true,
            'data' => $fees
        ]);
    }
}

PHP

<?php
// app/Http/Controllers/Api/V1/StatusController.php

namespace App\Http\Controllers\Api\V1;

use App\Http\Controllers\Controller;
use App\Http\Requests\Api\UpdateStatusRequest;
use App\Models\Application;
use App\Models\StatusHistory;
use App\Events\ApplicationApproved;
use App\Events\ApplicationRejected;
use App\Jobs\SendApprovalNotification;
use App\Services\Application\ApplicationService;
use Illuminate\Http\JsonResponse;
use Illuminate\Support\Facades\DB;
use Illuminate\Support\Facades\Log;

class StatusController extends Controller
{
    public function __construct(
        private ApplicationService $applicationService
    ) {}

    /**
     * Cập nhật trạng thái hồ sơ từ ASP
     * 
     * POST /api/v1/update-status
     * 
     * Body:
     * {
     *   "application_id": 123,
     *   "status": "approved",
     *   "student_code": "STU2024001",  // Chỉ khi approved
     *   "notes": "Hồ sơ hợp lệ",
     *   "changed_by": "admin@school.edu"
     * }
     */
    public function update(UpdateStatusRequest $request): JsonResponse
    {
        $validated = $request->validated();

        try {
            DB::beginTransaction();

            $application = Application::findOrFail($validated['application_id']);
            $oldStatus = $application->status;

            // Cập nhật trạng thái
            $application->status = $validated['status'];
            $application->admin_notes = $validated['notes'] ?? null;

            // Nếu approved, cấp mã sinh viên
            if ($validated['status'] === 'approved') {
                $application->approved_at = now();
                $application->approved_by = $validated['changed_by'] ?? 'system';
                
                if (isset($validated['student_code'])) {
                    $application->student->update([
                        'student_code' => $validated['student_code']
                    ]);
                }
            }

            // Nếu rejected hoặc request_info
            if (in_array($validated['status'], ['rejected', 'request_info'])) {
                $application->rejection_reason = $validated['notes'] ?? null;
            }

            $application->save();

            // Lưu lịch sử thay đổi
            StatusHistory::create([
                'application_id' => $application->id,
                'from_status' => $oldStatus,
                'to_status' => $validated['status'],
                'changed_by' => $validated['changed_by'] ?? 'ASP System',
                'notes' => $validated['notes'] ?? null,
                'source' => 'asp'
            ]);

            DB::commit();

            // Dispatch events và jobs
            match($validated['status']) {
                'approved' => event(new ApplicationApproved($application)),
                'rejected' => event(new ApplicationRejected($application)),
                'request_info' => SendApprovalNotification::dispatch($application, 'request_info'),
                default => null
            };

            Log::info('Application status updated via API', [
                'application_id' => $application->id,
                'old_status' => $oldStatus,
                'new_status' => $validated['status'],
                'changed_by' => $validated['changed_by'] ?? 'system'
            ]);

            return response()->json([
                'success' => true,
                'message' => 'Status updated successfully',
                'data' => [
                    'application_id' => $application->id,
                    'old_status' => $oldStatus,
                    'new_status' => $application->status,
                    'student_code' => $application->student->student_code
                ]
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            
            Log::error('Failed to update application status', [
                'application_id' => $validated['application_id'] ?? null,
                'error' => $e->getMessage()
            ]);

            return response()->json([
                'success' => false,
                'message' => 'Failed to update status',
                'error' => $e->getMessage()
            ], 500);
        }
    }

    /**
     * Cập nhật hàng loạt
     * 
     * POST /api/v1/bulk-update-status
     */
    public function bulkUpdate(Request $request): JsonResponse
    {
        $validated = $request->validate([
            'applications' => 'required|array|min:1|max:50',
            'applications.*.application_id' => 'required|exists:applications,id',
            'applications.*.status' => 'required|in:approved,rejected,request_info',
            'applications.*.student_code' => 'nullable|string',
            'applications.*.notes' => 'nullable|string',
            'changed_by' => 'required|string'
        ]);

        $results = [];

        foreach ($validated['applications'] as $item) {
            try {
                $item['changed_by'] = $validated['changed_by'];
                $this->update(new UpdateStatusRequest($item));
                $results[] = [
                    'application_id' => $item['application_id'],
                    'success' => true
                ];
            } catch (\Exception $e) {
                $results[] = [
                    'application_id' => $item['application_id'],
                    'success' => false,
                    'error' => $e->getMessage()
                ];
            }
        }

        return response()->json([
            'success' => true,
            'data' => $results
        ]);
    }
}

4.3 API Authentication Middleware

PHP

<?php
// app/Http/Middleware/ApiAuthentication.php

namespace App\Http\Middleware;

use App\Models\ApiLog;
use Closure;
use Illuminate\Http\Request;
use Symfony\Component\HttpFoundation\Response;

class ApiAuthentication
{
    /**
     * Xác thực API key từ ASP System
     */
    public function handle(Request $request, Closure $next): Response
    {
        $apiKey = $request->header('X-API-Key');
        $apiSecret = $request->header('X-API-Secret');
        $timestamp = $request->header('X-Timestamp');
        $signature = $request->header('X-Signature');

        // Validate API Key
        if (!$apiKey || $apiKey !== config('asp_integration.api_key')) {
            $this->logRequest($request, 401, 'Invalid API Key');
            return response()->json([
                'success' => false,
                'message' => 'Unauthorized: Invalid API Key'
            ], 401);
        }

        // Validate Timestamp (chống replay attack)
        if ($timestamp) {
            $requestTime = (int) $timestamp;
            $currentTime = time();
            $tolerance = 300; // 5 phút

            if (abs($currentTime - $requestTime) > $tolerance) {
                $this->logRequest($request, 401, 'Request expired');
                return response()->json([
                    'success' => false,
                    'message' => 'Unauthorized: Request expired'
                ], 401);
            }
        }

        // Validate Signature (HMAC)
        if (config('asp_integration.verify_signature', true)) {
            $payload = $request->getContent();
            $expectedSignature = hash_hmac(
                'sha256',
                $timestamp . $payload,
                config('asp_integration.api_secret')
            );

            if (!hash_equals($expectedSignature, $signature ?? '')) {
                $this->logRequest($request, 401, 'Invalid signature');
                return response()->json([
                    'success' => false,
                    'message' => 'Unauthorized: Invalid signature'
                ], 401);
            }
        }

        // IP Whitelist (optional)
        $allowedIps = config('asp_integration.allowed_ips', []);
        if (!empty($allowedIps) && !in_array($request->ip(), $allowedIps)) {
            $this->logRequest($request, 403, 'IP not allowed');
            return response()->json([
                'success' => false,
                'message' => 'Forbidden: IP not allowed'
            ], 403);
        }

        // Log successful request
        $this->logRequest($request, null, null);

        $response = $next($request);

        // Log response
        $this->updateLogWithResponse($request, $response);

        return $response;
    }

    private function logRequest(Request $request, ?int $statusCode, ?string $error): void
    {
        ApiLog::create([
            'endpoint' => $request->path(),
            'method' => $request->method(),
            'request_body' => $request->getContent(),
            'status_code' => $statusCode,
            'ip_address' => $request->ip(),
            'api_key' => substr($request->header('X-API-Key', ''), 0, 10) . '...',
            'error_message' => $error
        ]);
    }

    private function updateLogWithResponse(Request $request, Response $response): void
    {
        ApiLog::where('endpoint', $request->path())
              ->where('method', $request->method())
              ->whereNull('response_body')
              ->latest()
              ->first()
              ?->update([
                  'response_body' => $response->getContent(),
                  'status_code' => $response->getStatusCode()
              ]);
    }
}

⚙️ PHẦN 5: SERVICES
5.1 OTP Service

PHP

<?php
// app/Services/Auth/OtpService.php

namespace App\Services\Auth;

use App\Models\Otp;
use App\Jobs\SendOtpEmail;
use App\Jobs\SendOtpSms;
use App\Exceptions\OtpExpiredException;
use Illuminate\Support\Facades\Cache;
use Carbon\Carbon;

class OtpService
{
    private int $otpLength;
    private int $expiryMinutes;
    private int $maxAttempts;
    private int $resendCooldown;

    public function __construct()
    {
        $this->otpLength = config('otp.length', 6);
        $this->expiryMinutes = config('otp.expiry_minutes', 10);
        $this->maxAttempts = config('otp.max_attempts', 3);
        $this->resendCooldown = config('otp.resend_cooldown', 60);
    }

    /**
     * Tạo và gửi OTP
     */
    public function generate(
        string $identifier, 
        string $type, 
        string $purpose = 'registration',
        ?int $userId = null
    ): array {
        // Kiểm tra rate limit
        $cacheKey = "otp_cooldown:{$identifier}";
        if (Cache::has($cacheKey)) {
            $remainingSeconds = Cache::get($cacheKey) - time();
            return [
                'success' => false,
                'message' => "Please wait {$remainingSeconds} seconds before requesting new OTP",
                'cooldown' => $remainingSeconds
            ];
        }

        // Vô hiệu hóa OTP cũ
        Otp::where('identifier', $identifier)
           ->where('purpose', $purpose)
           ->whereNull('verified_at')
           ->update(['verified_at' => now()]); // Mark as used

        // Tạo OTP mới
        $otpCode = $this->generateCode();
        
        $otp = Otp::create([
            'user_id' => $userId,
            'identifier' => $identifier,
            'otp_code' => $otpCode,
            'type' => $type,
            'purpose' => $purpose,
            'expires_at' => Carbon::now()->addMinutes($this->expiryMinutes),
            'attempts' => 0
        ]);

        // Gửi OTP
        if ($type === 'email') {
            SendOtpEmail::dispatch($identifier, $otpCode, $purpose);
        } else {
            SendOtpSms::dispatch($identifier, $otpCode, $purpose);
        }

        // Set cooldown
        Cache::put($cacheKey, time() + $this->resendCooldown, $this->resendCooldown);

        return [
            'success' => true,
            'message' => "OTP sent to {$type}",
            'expires_in' => $this->expiryMinutes * 60,
            'type' => $type
        ];
    }

    /**
     * Xác thực OTP
     */
    public function verify(string $identifier, string $otpCode, string $purpose = 'registration'): array
    {
        $otp = Otp::where('identifier', $identifier)
                  ->where('purpose', $purpose)
                  ->whereNull('verified_at')
                  ->where('expires_at', '>', now())
                  ->latest()
                  ->first();

        if (!$otp) {
            return [
                'success' => false,
                'message' => 'OTP not found or expired'
            ];
        }

        // Kiểm tra số lần thử
        if ($otp->attempts >= $this->maxAttempts) {
            return [
                'success' => false,
                'message' => 'Maximum attempts exceeded. Please request new OTP'
            ];
        }

        // Kiểm tra OTP
        if ($otp->otp_code !== $otpCode) {
            $otp->increment('attempts');
            $remainingAttempts = $this->maxAttempts - $otp->attempts;
            
            return [
                'success' => false,
                'message' => "Invalid OTP. {$remainingAttempts} attempts remaining"
            ];
        }

        // OTP hợp lệ
        $otp->update(['verified_at' => now()]);

        return [
            'success' => true,
            'message' => 'OTP verified successfully',
            'user_id' => $otp->user_id
        ];
    }

    /**
     * Tạo mã OTP ngẫu nhiên
     */
    private function generateCode(): string
    {
        $min = pow(10, $this->otpLength - 1);
        $max = pow(10, $this->otpLength) - 1;
        return (string) random_int($min, $max);
    }
}

5.2 Application Service

PHP

<?php
// app/Services/Application/ApplicationService.php

namespace App\Services\Application;

use App\Models\Application;
use App\Models\ApplicationStep;
use App\Models\Student;
use App\Enums\ApplicationStatus;
use Illuminate\Support\Facades\DB;
use Illuminate\Support\Str;

class ApplicationService
{
    private array $stepConfig;

    public function __construct()
    {
        $this->stepConfig = config('admission.steps', [
            1 => ['name' => 'personal_info', 'label' => 'Personal Information'],
            2 => ['name' => 'parent_info', 'label' => 'Parent/Guardian Information'],
            3 => ['name' => 'program_selection', 'label' => 'Program Selection'],
            4 => ['name' => 'documents', 'label' => 'Document Upload'],
        ]);
    }

    /**
     * Tạo application mới
     */
    public function create(int $userId): Application
    {
        return DB::transaction(function () use ($userId) {
            // Tạo student record nếu chưa có
            $student = Student::firstOrCreate(
                ['user_id' => $userId],
                [
                    'first_name' => '',
                    'last_name' => '',
                    'date_of_birth' => now(),
                    'gender' => 'other',
                    'address' => '',
                    'city' => ''
                ]
            );

            // Tạo application
            $application = Application::create([
                'student_id' => $student->id,
                'program_id' => null,
                'block_id' => null,
                'application_number' => $this->generateApplicationNumber(),
                'status' => ApplicationStatus::DRAFT,
                'current_step' => 1,
                'total_steps' => count($this->stepConfig)
            ]);

            // Tạo các bước
            foreach ($this->stepConfig as $stepNumber => $config) {
                ApplicationStep::create([
                    'application_id' => $application->id,
                    'step_number' => $stepNumber,
                    'step_name' => $config['name'],
                    'data' => [],
                    'is_completed' => false
                ]);
            }

            return $application;
        });
    }

    /**
     * Lưu dữ liệu một bước
     */
    public function saveStep(Application $application, int $stepNumber, array $data): ApplicationStep
    {
        $step = ApplicationStep::where('application_id', $application->id)
                               ->where('step_number', $stepNumber)
                               ->firstOrFail();

        $step->update([
            'data' => array_merge($step->data ?? [], $data),
            'is_completed' => true,
            'completed_at' => now()
        ]);

        // Cập nhật thông tin student nếu cần
        $this->updateStudentFromStep($application->student, $stepNumber, $data);

        // Cập nhật current_step
        if ($stepNumber >= $application->current_step) {
            $application->update(['current_step' => min($stepNumber + 1, $application->total_steps)]);
        }

        return $step->fresh();
    }

    /**
     * Nộp hồ sơ
     */
    public function submit(Application $application): Application
    {
        // Kiểm tra đã hoàn thành tất cả các bước
        $incompleteSteps = $application->steps()->where('is_completed', false)->count();
        
        if ($incompleteSteps > 0) {
            throw new \Exception("Please complete all steps before submitting");
        }

        // Kiểm tra đã thanh toán
        if (!$application->payment || $application->payment->status !== 'submitted') {
            throw new \Exception("Please submit payment proof before submitting application");
        }

        $application->update([
            'status' => ApplicationStatus::PENDING_APPROVAL,
            'submitted_at' => now()
        ]);

        // Dispatch event
        event(new \App\Events\ApplicationSubmitted($application));

        return $application->fresh();
    }

    /**
     * Tạo mã hồ sơ
     */
    private function generateApplicationNumber(): string
    {
        $year = date('Y');
        $random = strtoupper(Str::random(6));
        $number = "APP-{$year}-{$random}";

        // Đảm bảo unique
        while (Application::where('application_number', $number)->exists()) {
            $random = strtoupper(Str::random(6));
            $number = "APP-{$year}-{$random}";
        }

        return $number;
    }

    /**
     * Cập nhật thông tin student từ step data
     */
    private function updateStudentFromStep(Student $student, int $stepNumber, array $data): void
    {
        switch ($stepNumber) {
            case 1: // Personal Info
                $student->update([
                    'first_name' => $data['first_name'] ?? $student->first_name,
                    'middle_name' => $data['middle_name'] ?? null,
                    'last_name' => $data['last_name'] ?? $student->last_name,
                    'date_of_birth' => $data['date_of_birth'] ?? $student->date_of_birth,
                    'gender' => $data['gender'] ?? $student->gender,
                    'nationality' => $data['nationality'] ?? 'Kenya',
                    'national_id' => $data['national_id'] ?? null,
                    'address' => $data['address'] ?? $student->address,
                    'city' => $data['city'] ?? $student->city,
                    'county' => $data['county'] ?? null,
                ]);
                break;

            case 2: // Parent Info
                $student->parentInfo()->updateOrCreate(
                    ['student_id' => $student->id],
                    [
                        'relation_type' => $data['relation_type'] ?? 'parent',
                        'full_name' => $data['parent_name'] ?? '',
                        'phone' => $data['parent_phone'] ?? '',
                        'email' => $data['parent_email'] ?? null,
                        'occupation' => $data['parent_occupation'] ?? null,
                        'address' => $data['parent_address'] ?? '',
                    ]
                );
                break;
        }
    }
}

5.3 ASP Integration Service

PHP

<?php
// app/Services/Integration/AspApiService.php

namespace App\Services\Integration;

use Illuminate\Support\Facades\Http;
use Illuminate\Support\Facades\Log;
use Illuminate\Support\Facades\Cache;

class AspApiService
{
    private string $baseUrl;
    private string $apiKey;
    private string $apiSecret;
    private int $timeout;

    public function __construct()
    {
        $this->baseUrl = config('asp_integration.base_url');
        $this->apiKey = config('asp_integration.api_key');
        $this->apiSecret = config('asp_integration.api_secret');
        $this->timeout = config('asp_integration.timeout', 30);
    }

    /**
     * Lấy điểm sinh viên từ ASP
     */
    public function getStudentGrades(string $studentCode): array
    {
        $cacheKey = "grades:{$studentCode}";
        
        return Cache::remember($cacheKey, 300, function () use ($studentCode) {
            $response = $this->makeRequest('GET', "/students/{$studentCode}/grades");
            return $response['data'] ?? [];
        });
    }

    /**
     * Lấy thời khóa biểu từ ASP
     */
    public function getStudentTimetable(string $studentCode): array
    {
        $cacheKey = "timetable:{$studentCode}";
        
        return Cache::remember($cacheKey, 600, function () use ($studentCode) {
            $response = $this->makeRequest('GET', "/students/{$studentCode}/timetable");
            return $response['data'] ?? [];
        });
    }

    /**
     * Lấy công nợ học phí từ ASP
     */
    public function getStudentFees(string $studentCode): array
    {
        $cacheKey = "fees:{$studentCode}";
        
        return Cache::remember($cacheKey, 300, function () use ($studentCode) {
            $response = $this->makeRequest('GET', "/students/{$studentCode}/fees");
            return $response['data'] ?? [];
        });
    }

    /**
     * Thực hiện request đến ASP API
     */
    private function makeRequest(string $method, string $endpoint, array $data = []): array
    {
        $timestamp = time();
        $url = rtrim($this->baseUrl, '/') . $endpoint;

        try {
            $response = Http::withHeaders($this->buildHeaders($timestamp, $data))
                           ->timeout($this->timeout)
                           ->$method($url, $data);

            if ($response->successful()) {
                return $response->json();
            }

            Log::error('ASP API request failed', [
                'endpoint' => $endpoint,
                'status' => $response->status(),
                'body' => $response->body()
            ]);

            return [
                'success' => false,
                'error' => 'API request failed',
                'status' => $response->status()
            ];

        } catch (\Exception $e) {
            Log::error('ASP API connection error', [
                'endpoint' => $endpoint,
                'error' => $e->getMessage()
            ]);

            return [
                'success' => false,
                'error' => 'Connection failed',
                'message' => $e->getMessage()
            ];
        }
    }

    /**
     * Xây dựng headers cho request
     */
    private function buildHeaders(int $timestamp, array $data): array
    {
        $payload = json_encode($data);
        $signature = hash_hmac('sha256', $timestamp . $payload, $this->apiSecret);

        return [
            'X-API-Key' => $this->apiKey,
            'X-Timestamp' => $timestamp,
            'X-Signature' => $signature,
            'Content-Type' => 'application/json',
            'Accept' => 'application/json'
        ];
    }
}

🖥️ PHẦN 6: FRONTEND VIEWS
6.1 Application Wizard (Livewire Component)

PHP

<?php
// app/Livewire/ApplicationWizard.php

namespace App\Livewire;

use Livewire\Component;
use Livewire\WithFileUploads;
use App\Models\Application;
use App\Services\Application\ApplicationService;
use App\Services\Application\DocumentService;

class ApplicationWizard extends Component
{
    use WithFileUploads;

    public Application $application;
    public int $currentStep = 1;
    public array $stepData = [];
    
    // Step 1: Personal Info
    public string $first_name = '';
    public string $middle_name = '';
    public string $last_name = '';
    public string $date_of_birth = '';
    public string $gender = '';
    public string $nationality = 'Kenya';
    public string $national_id = '';
    public string $address = '';
    public string $city = '';
    public string $county = '';
    
    // Step 2: Parent Info
    public string $parent_name = '';
    public string $parent_phone = '';
    public string $parent_email = '';
    public string $parent_occupation = '';
    public string $parent_address = '';
    public string $relation_type = 'parent';
    
    // Step 3: Program Selection
    public ?int $program_id = null;
    public ?int $block_id = null;
    
    // Step 4: Documents
    public $kcpe_certificate;
    public $birth_certificate;
    public $medical_certificate;
    public $passport_photo;

    protected $listeners = ['refreshApplication'];

    public function mount(Application $application)
    {
        $this->application = $application;
        $this->currentStep = $application->current_step;
        $this->loadStepData();
    }

    public function render()
    {
        return view('livewire.application-wizard', [
            'programs' => \App\Models\Program::active()->get(),
            'blocks' => \App\Models\AcademicBlock::active()->get(),
            'steps' => [
                1 => 'Personal Information',
                2 => 'Parent/Guardian',
                3 => 'Program Selection',
                4 => 'Documents'
            ]
        ]);
    }

    public function nextStep()
    {
        $this->validateCurrentStep();
        $this->saveCurrentStep();
        
        if ($this->currentStep < 4) {
            $this->currentStep++;
        }
    }

    public function previousStep()
    {
        if ($this->currentStep > 1) {
            $this->currentStep--;
        }
    }

    public function goToStep(int $step)
    {
        // Chỉ cho phép quay lại hoặc đi tới step đã hoàn thành
        if ($step <= $this->application->current_step) {
            $this->currentStep = $step;
        }
    }

    private function validateCurrentStep()
    {
        $rules = match($this->currentStep) {
            1 => [
                'first_name' => 'required|string|max:100',
                'last_name' => 'required|string|max:100',
                'date_of_birth' => 'required|date|before:-14 years',
                'gender' => 'required|in:male,female,other',
                'national_id' => 'nullable|string|max:50',
                'address' => 'required|string',
                'city' => 'required|string|max:100',
            ],
            2 => [
                'parent_name' => 'required|string|max:200',
                'parent_phone' => 'required|string|max:20',
                'parent_email' => 'nullable|email',
                'relation_type' => 'required|in:parent,guardian,other',
            ],
            3 => [
                'program_id' => 'required|exists:programs,id',
                'block_id' => 'required|exists:academic_blocks,id',
            ],
            4 => [
                'kcpe_certificate' => 'required|file|mimes:pdf,jpg,jpeg,png|max:5120',
                'birth_certificate' => 'required|file|mimes:pdf,jpg,jpeg,png|max:5120',
                'passport_photo' => 'required|image|max:2048',
            ],
            default => []
        };

        $this->validate($rules);
    }

    private function saveCurrentStep()
    {
        $service = app(ApplicationService::class);
        
        $data = match($this->currentStep) {
            1 => [
                'first_name' => $this->first_name,
                'middle_name' => $this->middle_name,
                'last_name' => $this->last_name,
                'date_of_birth' => $this->date_of_birth,
                'gender' => $this->gender,
                'nationality' => $this->nationality,
                'national_id' => $this->national_id,
                'address' => $this->address,
                'city' => $this->city,
                'county' => $this->county,
            ],
            2 => [
                'parent_name' => $this->parent_name,
                'parent_phone' => $this->parent_phone,
                'parent_email' => $this->parent_email,
                'parent_occupation' => $this->parent_occupation,
                'parent_address' => $this->parent_address,
                'relation_type' => $this->relation_type,
            ],
            3 => [
                'program_id' => $this->program_id,
                'block_id' => $this->block_id,
            ],
            4 => $this->uploadDocuments(),
            default => []
        };

        $service->saveStep($this->application, $this->currentStep, $data);
        
        // Update application nếu là step 3
        if ($this->currentStep === 3) {
            $this->application->update([
                'program_id' => $this->program_id,
                'block_id' => $this->block_id
            ]);
        }

        $this->application->refresh();
    }

    private function uploadDocuments(): array
    {
        $documentService = app(DocumentService::class);
        $documents = [];

        if ($this->kcpe_certificate) {
            $documents['kcpe_certificate'] = $documentService->upload(
                $this->application,
                $this->kcpe_certificate,
                'kcpe_certificate'
            );
        }

        if ($this->birth_certificate) {
            $documents['birth_certificate'] = $documentService->upload(
                $this->application,
                $this->birth_certificate,
                'birth_certificate'
            );
        }

        if ($this->passport_photo) {
            $documents['passport_photo'] = $documentService->upload(
                $this->application,
                $this->passport_photo,
                'passport_photo'
            );
        }

        return $documents;
    }

    private function loadStepData()
    {
        $student = $this->application->student;
        
        // Load personal info
        $this->first_name = $student->first_name ?? '';
        $this->last_name = $student->last_name ?? '';
        $this->date_of_birth = $student->date_of_birth?->format('Y-m-d') ?? '';
        $this->gender = $student->gender ?? '';
        $this->address = $student->address ?? '';
        $this->city = $student->city ?? '';
        
        // Load parent info
        if ($parent = $student->parentInfo) {
            $this->parent_name = $parent->full_name ?? '';
            $this->parent_phone = $parent->phone ?? '';
            $this->parent_email = $parent->email ?? '';
        }
        
        // Load program selection
        $this->program_id = $this->application->program_id;
        $this->block_id = $this->application->block_id;
    }
}

6.2 Application Wizard Blade View

blade

{{-- resources/views/livewire/application-wizard.blade.php --}}

<div class="max-w-4xl mx-auto p-6">
    {{-- Step Indicator --}}
    <div class="mb-8">
        <div class="flex items-center justify-between">
            @foreach($steps as $stepNumber => $stepName)
                <div class="flex items-center {{ $stepNumber < count($steps) ? 'flex-1' : '' }}">
                    <button 
                        wire:click="goToStep({{ $stepNumber }})"
                        class="flex items-center justify-center w-10 h-10 rounded-full 
                               {{ $currentStep >= $stepNumber ? 'bg-blue-600 text-white' : 'bg-gray-200 text-gray-600' }}
                               {{ $stepNumber <= $application->current_step ? 'cursor-pointer hover:bg-blue-700' : 'cursor-not-allowed' }}"
                        {{ $stepNumber > $application->current_step ? 'disabled' : '' }}
                    >
                        @if($application->steps->where('step_number', $stepNumber)->first()?->is_completed)
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                            </svg>
                        @else
                            {{ $stepNumber }}
                        @endif
                    </button>
                    
                    <span class="ml-2 text-sm font-medium {{ $currentStep == $stepNumber ? 'text-blue-600' : 'text-gray-500' }}">
                        {{ $stepName }}
                    </span>
                    
                    @if($stepNumber < count($steps))
                        <div class="flex-1 h-0.5 mx-4 {{ $currentStep > $stepNumber ? 'bg-blue-600' : 'bg-gray-200' }}"></div>
                    @endif
                </div>
            @endforeach
        </div>
    </div>

    {{-- Form Content --}}
    <div class="bg-white rounded-lg shadow-lg p-6">
        <form wire:submit.prevent="nextStep">
            
            {{-- Step 1: Personal Information --}}
            @if($currentStep === 1)
                <h2 class="text-xl font-bold mb-6">Personal Information</h2>
                
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">First Name *</label>
                        <input type="text" wire:model="first_name" 
                               class="w-full border rounded-lg px-4 py-2 focus:ring-2 focus:ring-blue-500 @error('first_name') border-red-500 @enderror">
                        @error('first_name') <span class="text-red-500 text-sm">{{ $message }}</span> @enderror
                    </div>
                    
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Middle Name</label>
                        <input type="text" wire:model="middle_name" class="w-full border rounded-lg px-4 py-2">
                    </div>
                    
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Last Name *</label>
                        <input type="text" wire:model="last_name" 
                               class="w-full border rounded-lg px-4 py-2 @error('last_name') border-red-500 @enderror">
                        @error('last_name') <span class="text-red-500 text-sm">{{ $message }}</span> @enderror
                    </div>
                    
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Date of Birth *</label>
                        <input type="date" wire:model="date_of_birth" 
                               class="w-full border rounded-lg px-4 py-2 @error('date_of_birth') border-red-500 @enderror">
                        @error('date_of_birth') <span class="text-red-500 text-sm">{{ $message }}</span> @enderror
                    </div>
                    
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Gender *</label>
                        <select wire:model="gender" class="w-full border rounded-lg px-4 py-2 @error('gender') border-red-500 @enderror">
                            <option value="">Select Gender</option>
                            <option value="male">Male</option>
                            <option value="female">Female</option>
                            <option value="other">Other</option>
                        </select>
                        @error('gender') <span class="text-red-500 text-sm">{{ $message }}</span> @enderror
                    </div>
                    
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">National ID</label>
                        <input type="text" wire:model="national_id" class="w-full border rounded-lg px-4 py-2">
                    </div>
                    
                    <div class="md:col-span-2">
                        <label class="block text-sm font-medium text-gray-700 mb-1">Address *</label>
                        <textarea wire:model="address" rows="2" 
                                  class="w-full border rounded-lg px-4 py-2 @error('address') border-red-500 @enderror"></textarea>
                        @error('address') <span class="text-red-500 text-sm">{{ $message }}</span> @enderror
                    </div>
                    
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">City *</label>
                        <input type="text" wire:model="city" 
                               class="w-full border rounded-lg px-4 py-2 @error('city') border-red-500 @enderror">
                        @error('city') <span class="text-red-500 text-sm">{{ $message }}</span> @enderror
                    </div>
                    
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">County</label>
                        <input type="text" wire:model="county" class="w-full border rounded-lg px-4 py-2">
                    </div>
                </div>
            @endif

            {{-- Step 2: Parent Information --}}
            @if($currentStep === 2)
                <h2 class="text-xl font-bold mb-6">Parent/Guardian Information</h2>
                
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Relationship *</label>
                        <select wire:model="relation_type" class="w-full border rounded-lg px-4 py-2">
                            <option value="parent">Parent</option>
                            <option value="guardian">Guardian</option>
                            <option value="other">Other</option>
                        </select>
                    </div>
                    
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Full Name *</label>
                        <input type="text" wire:model="parent_name" 
                               class="w-full border rounded-lg px-4 py-2 @error('parent_name') border-red-500 @enderror">
                        @error('parent_name') <span class="text-red-500 text-sm">{{ $message }}</span> @enderror
                    </div>
                    
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Phone Number *</label>
                        <input type="tel" wire:model="parent_phone" placeholder="+254..."
                               class="w-full border rounded-lg px-4 py-2 @error('parent_phone') border-red-500 @enderror">
                        @error('parent_phone') <span class="text-red-500 text-sm">{{ $message }}</span> @enderror
                    </div>
                    
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Email</label>
                        <input type="email" wire:model="parent_email" class="w-full border rounded-lg px-4 py-2">
                    </div>
                    
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Occupation</label>
                        <input type="text" wire:model="parent_occupation" class="w-full border rounded-lg px-4 py-2">
                    </div>
                    
                    <div class="md:col-span-2">
                        <label class="block text-sm font-medium text-gray-700 mb-1">Address</label>
                        <textarea wire:model="parent_address" rows="2" class="w-full border rounded-lg px-4 py-2"></textarea>
                    </div>
                </div>
            @endif

            {{-- Step 3: Program Selection --}}
            @if($currentStep === 3)
                <h2 class="text-xl font-bold mb-6">Program Selection</h2>
                
                <div class="grid grid-cols-1 gap-6">
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">Select Program *</label>
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                            @foreach($programs as $program)
                                <label class="relative flex cursor-pointer rounded-lg border p-4 
                                             {{ $program_id == $program->id ? 'border-blue-500 bg-blue-50' : 'border-gray-200 hover:border-blue-300' }}">
                                    <input type="radio" wire:model="program_id" value="{{ $program->id }}" class="sr-only">
                                    <div class="flex flex-1">
                                        <div class="flex flex-col">
                                            <span class="block text-sm font-medium text-gray-900">{{ $program->name }}</span>
                                            <span class="mt-1 text-sm text-gray-500">{{ $program->duration }}</span>
                                            <span class="mt-2 text-sm font-medium text-blue-600">KES {{ number_format($program->fee) }}</span>
                                        </div>
                                    </div>
                                </label>
                            @endforeach
                        </div>
                        @error('program_id') <span class="text-red-500 text-sm">{{ $message }}</span> @enderror
                    </div>
                    
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">Select Intake *</label>
                        <select wire:model="block_id" class="w-full border rounded-lg px-4 py-2 @error('block_id') border-red-500 @enderror">
                            <option value="">Choose intake period</option>
                            @foreach($blocks as $block)
                                <option value="{{ $block->id }}">{{ $block->name }} - {{ $block->intake }} {{ $block->year }}</option>
                            @endforeach
                        </select>
                        @error('block_id') <span class="text-red-500 text-sm">{{ $message }}</span> @enderror
                    </div>
                </div>
            @endif

            {{-- Step 4: Documents --}}
            @if($currentStep === 4)
                <h2 class="text-xl font-bold mb-6">Upload Documents</h2>
                
                <div class="space-y-6">
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">KCPE Certificate *</label>
                        <div class="mt-1 flex justify-center px-6 pt-5 pb-6 border-2 border-gray-300 border-dashed rounded-lg 
                                    @error('kcpe_certificate') border-red-500 @enderror">
                            <div class="space-y-1 text-center">
                                @if($kcpe_certificate)
                                    <p class="text-sm text-green-600">✓ {{ $kcpe_certificate->getClientOriginalName() }}</p>
                                @else
                                    <svg class="mx-auto h-12 w-12 text-gray-400" stroke="currentColor" fill="none" viewBox="0 0 48 48">
                                        <path d="M28 8H12a4 4 0 00-4 4v20m32-12v8m0 0v8a4 4 0 01-4 4H12a4 4 0 01-4-4v-4m32-4l-3.172-3.172a4 4 0 00-5.656 0L28 28M8 32l9.172-9.172a4 4 0 015.656 0L28 28m0 0l4 4m4-24h8m-4-4v8m-12 4h.02" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
                                    </svg>
                                @endif
                                <div class="flex text-sm text-gray-600">
                                    <label class="relative cursor-pointer bg-white rounded-md font-medium text-blue-600 hover:text-blue-500">
                                        <span>Upload file</span>
                                        <input type="file" wire:model="kcpe_certificate" class="sr-only" accept=".pdf,.jpg,.jpeg,.png">
                                    </label>
                                </div>
                                <p class="text-xs text-gray-500">PDF, JPG, PNG up to 5MB</p>
                            </div>
                        </div>
                        @error('kcpe_certificate') <span class="text-red-500 text-sm">{{ $message }}</span> @enderror
                    </div>
                    
                    {{-- Similar upload fields for birth_certificate, passport_photo, medical_certificate --}}
                </div>
            @endif

            {{-- Navigation Buttons --}}
            <div class="flex justify-between mt-8 pt-6 border-t">
                @if($currentStep > 1)
                    <button type="button" wire:click="previousStep" 
                            class="px-6 py-2 border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-50">
                        ← Previous
                    </button>
                @else
                    <div></div>
                @endif
                
                <button type="submit" 
                        class="px-6 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 flex items-center"
                        wire:loading.attr="disabled"
                        wire:loading.class="opacity-50 cursor-not-allowed">
                    <span wire:loading.remove wire:target="nextStep">
                        {{ $currentStep === 4 ? 'Continue to Payment' : 'Next Step →' }}
                    </span>
                    <span wire:loading wire:target="nextStep">
                        Processing...
                    </span>
                </button>
            </div>
        </form>
    </div>
</div>

📅 PHẦN 7: TIMELINE DỰ ÁN

text

╔══════════════════════════════════════════════════════════════════════════════╗
║                        TIMELINE DỰ ÁN - 12 TUẦN                              ║
╠══════════════════════════════════════════════════════════════════════════════╣

GIAI ĐOẠN 1: SETUP & CƠ SỞ (Tuần 1-2)
├── Tuần 1
│   ├── ☐ Setup môi trường development (Docker, Laravel)
│   ├── ☐ Thiết kế database schema chi tiết
│   ├── ☐ Tạo migrations và seeders
│   └── ☐ Setup CI/CD cơ bản
│
├── Tuần 2
│   ├── ☐ Xây dựng Models và Relationships
│   ├── ☐ Tạo Enums và Traits
│   ├── ☐ Setup Authentication với Laravel Breeze/Jetstream
│   └── ☐ Cấu hình Redis, Queue

GIAI ĐOẠN 2: MODULE ĐĂNG KÝ & OTP (Tuần 3-4)
├── Tuần 3
│   ├── ☐ OTP Service (Email + SMS)
│   ├── ☐ Register Controller & Views
│   ├── ☐ Login với OTP
│   └── ☐ Rate limiting cho OTP
│
├── Tuần 4
│   ├── ☐ Email templates cho OTP
│   ├── ☐ SMS integration (Africa's Talking / Twilio)
│   ├── ☐ Unit tests cho Auth module
│   └── ☐ Password reset flow

GIAI ĐOẠN 3: MODULE APPLICATION FORM (Tuần 5-7)
├── Tuần 5
│   ├── ☐ Application Service
│   ├── ☐ Step 1 & 2: Personal & Parent Info
│   ├── ☐ Livewire Application Wizard
│   └── ☐ Form validation
│
├── Tuần 6
│   ├── ☐ Step 3: Program Selection
│   ├── ☐ Step 4: Document Upload
│   ├── ☐ Document Service (storage, validation)
│   └── ☐ File upload với progress bar
│
├── Tuần 7
│   ├── ☐ Payment Module (M-Pesa integration)
│   ├── ☐ Payment proof upload
│   ├── ☐ Application submission flow
│   └── ☐ Email notifications (submission confirmation)

GIAI ĐOẠN 4: API CHO ASP.NET (Tuần 8-9)
├── Tuần 8
│   ├── ☐ API Authentication Middleware
│   ├── ☐ Student API (GET endpoints)
│   ├── ☐ Document download API
│   └── ☐ API Rate limiting & logging
│
├── Tuần 9
│   ├── ☐ Status Update API (POST endpoints)
│   ├── ☐ Bulk update API
│   ├── ☐ Webhook receivers
│   └── ☐ API Documentation (Swagger/OpenAPI)

GIAI ĐOẠN 5: STUDENT PORTAL (Tuần 10-11)
├── Tuần 10
│   ├── ☐ Student Dashboard (sau approval)
│   ├── ☐ Timetable view (fetch từ ASP)
│   ├── ☐ Grades view
│   └── ☐ Fee statement view
│
├── Tuần 11
│   ├── ☐ Profile management
│   ├── ☐ Application status tracking
│   ├── ☐ Notification center
│   └── ☐ Request info handling

GIAI ĐOẠN 6: TESTING & DEPLOYMENT (Tuần 12)
├── Tuần 12
│   ├── ☐ Integration testing
│   ├── ☐ UAT (User Acceptance Testing)
│   ├── ☐ Security audit
│   ├── ☐ Performance optimization
│   ├── ☐ Production deployment
│   └── ☐ Documentation hoàn chỉnh

╚══════════════════════════════════════════════════════════════════════════════╝

🔐 PHẦN 8: BẢO MẬT
8.1 Security Checklist

YAML

Authentication & Authorization:
  - [x] OTP verification cho đăng ký
  - [x] Rate limiting cho login attempts
  - [x] Session timeout configuration
  - [x] CSRF protection
  - [x] API key + HMAC signature cho ASP integration

Data Protection:
  - [x] Encrypt sensitive data at rest
  - [x] HTTPS enforced
  - [x] SQL injection prevention (Eloquent ORM)
  - [x] XSS protection (Blade auto-escaping)
  - [x] Input validation & sanitization

File Upload Security:
  - [x] File type validation (MIME + extension)
  - [x] File size limits
  - [x] Rename uploaded files
  - [x] Store outside public directory
  - [x] Virus scanning (optional)

API Security:
  - [x] API key authentication
  - [x] Request timestamp validation (anti-replay)
  - [x] IP whitelist for ASP server
  - [x] Request/Response logging
  - [x] Rate limiting per API key

Infrastructure:
  - [x] Firewall configuration
  - [x] Regular security updates
  - [x] Database backups
  - [x] Log monitoring
  - [x] Error handling (không expose stack traces)

8.2 Environment Configuration

env

# .env.example

APP_NAME="Student Admission Portal"
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://admissions.school.edu

# Database
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=admission_portal
DB_USERNAME=
DB_PASSWORD=

# Redis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Session
SESSION_DRIVER=redis
SESSION_LIFETIME=120

# Queue
QUEUE_CONNECTION=redis

# Mail
MAIL_MAILER=smtp
MAIL_HOST=
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="noreply@school.edu"
MAIL_FROM_NAME="${APP_NAME}"

# SMS Gateway
SMS_DRIVER=africas_talking
SMS_API_KEY=
SMS_USERNAME=

# M-Pesa
MPESA_CONSUMER_KEY=
MPESA_CONSUMER_SECRET=
MPESA_SHORTCODE=
MPESA_PASSKEY=
MPESA_ENV=production

# ASP Integration
ASP_API_BASE_URL=https://internal-asp.school.local/api
ASP_API_KEY=
ASP_API_SECRET=
ASP_ALLOWED_IPS="192.168.1.100,192.168.1.101"
ASP_VERIFY_SIGNATURE=true

# OTP Configuration
OTP_LENGTH=6
OTP_EXPIRY_MINUTES=10
OTP_MAX_ATTEMPTS=3
OTP_RESEND_COOLDOWN=60

# File Storage
FILESYSTEM_DISK=s3
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
